This is the Windows version of iDecrypt, so basically has the same features apart from the Mac OS X version doesn't have the update feature (YET).

Still uses VFDecrypt as the backend to process everything, well it is a G.U.I so you wouldn't expect any less!

- Enjoy!